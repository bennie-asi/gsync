<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="GSYNC.App.Primitives.InfoCallout"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border
        x:Name="CalloutBorder"
        Padding="12"
        Background="{StaticResource AppSurfaceAltBrush}"
        BorderThickness="0"
        CornerRadius="8">
        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border x:Name="AccentBar" CornerRadius="8" />

            <StackPanel Grid.Column="1" Spacing="4">
                <TextBlock FontWeight="SemiBold" Text="{Binding Title, ElementName=Root}" />
                <TextBlock Foreground="{StaticResource AppTextSecondaryBrush}" Text="{Binding Message, ElementName=Root}" TextWrapping="WrapWholeWords" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>

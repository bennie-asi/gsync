<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GSYNC.App.Pages.SyncTargetsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:primitives="using:GSYNC.App.Primitives">
    <Grid Padding="{StaticResource AppPagePadding}" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <primitives:SectionHeader Title="Sync Targets" Subtitle="共享目标列表与右侧属性编辑面板。" />
        <primitives:ToolbarFilterRow Grid.Row="1" />

        <Grid Grid.Row="2" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.3*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <primitives:DenseDataGrid />

            <StackPanel Grid.Column="1" Spacing="12">
                <primitives:Badge Text="Connected" />
                <primitives:PropertySheet />
                <primitives:InfoCallout />
                <InfoBar IsOpen="{Binding ShowFailureState}" Severity="Error" Message="连接失败状态将按 Stitch failure variant 继续细化。" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>

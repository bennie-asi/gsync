## 1. 收敛导航 rail 默认态与展开态

- [x] 1.1 对照 Stitch 导航视觉方向确认 rail 的默认窄图标态、展开态和设置按钮位置
- [x] 1.2 调整 AppNavRail 默认样式，使默认仅显示图标、宽度更窄且更接近设计稿
- [x] 1.3 为 AppNavRail 增加展开/收起控制，使点击后显示导航文字并扩展 rail 宽度

## 2. 调整壳层布局承载与设置按钮一致性

- [x] 2.1 修改 MainWindow 对导航 rail 宽度的承载方式，使主内容区域随 rail 状态切换稳定重排
- [x] 2.2 收敛设置按钮样式、间距和位置，使其与其余导航项同体系呈现
- [x] 2.3 保持 rail 展开/收起时顶部标题栏、底部状态栏和主内容布局一致，不引入覆盖式浮层分支

## 3. 完成对稿与验证

- [x] 3.1 逐项核对默认窄图标态是否符合用户要求：只显示图标、宽度较窄、设置按钮样式一致
- [x] 3.2 逐项核对展开态是否符合用户要求：文字展开显示、rail 宽度增加、主布局稳定
- [x] 3.3 运行构建验证，确认 MainWindow / AppNavRail 相关改动在 WinUI 项目中可编译通过